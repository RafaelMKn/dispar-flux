# Publicar imagens Linux para amd64 e arm64

A imagem OCI oficial suportará Linux `amd64` e `arm64`. Docker Desktop em Windows ou macOS poderá ser usado para avaliação e desenvolvimento, mas não fará parte da matriz oficial de produção.
