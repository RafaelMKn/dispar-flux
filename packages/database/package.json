{
  "name": "@dispar-flux/database",
  "version": "0.0.1",
  "private": true,
  "description": "Native SQLite database, migrations, and installation lock for Dispar Flux",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build && node scripts/copy-migrations.js",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/**/*.test.ts"
  },
  "dependencies": {
    "@dispar-flux/domain": "*"
  },
  "license": "AGPL-3.0-only"
}
